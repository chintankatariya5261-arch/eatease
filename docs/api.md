# APIs

- `GET /api/restaurants.php` returns active restaurants.
- `GET /api/menu_items.php?hotel_id=ID` returns menu items for a restaurant.

Responses include `success` and `data` fields for integration.
